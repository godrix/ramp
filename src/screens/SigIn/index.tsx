import React from 'react';
import { View } from 'react-native';
// import { Container } from './styles';

// type ChildrenReact<T = {}> = T & { children?: React.ReactNode };

const SigIn = () => {
  return <View />;
};

export default SigIn;
